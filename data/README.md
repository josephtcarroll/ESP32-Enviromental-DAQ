# Experimental Data

This directory contains CSV datasets recorded from the ESP32 during system characterization.

`step_response.csv` contains the controlled light step-response experiment used to evaluate raw and digitally filtered ADC response.
